// Stub file to fix import errors during transition to simple approach

export function fetchUSDCBalance() {
  throw new Error('fetchUSDCBalance no longer supported - use Web3Service.getUSDCBalance() instead');
}